# 배포 가이드 · Vercel 배포 + 카카오 애드핏 등록

이 문서는 받으신 ZIP 파일을 풀고 → Vercel에 배포하고 → 도메인 주소를 받아 → 카카오 애드핏에 매체를 등록하기까지의 전 과정을 안내합니다. 처음 하시는 분 기준으로 약 30분~1시간 걸립니다.

---

## 📁 파일 구조 미리보기

```
stockcheck/
├── app/
│   ├── api/check/route.js     ← 서버 API (Anthropic 호출)
│   ├── about/page.js          ← 소개 페이지
│   ├── privacy/page.js        ← 개인정보처리방침
│   ├── terms/page.js          ← 이용약관
│   ├── StockCheck.js          ← 메인 컴포넌트
│   ├── StaticPage.js          ← 정적 페이지 레이아웃
│   ├── globals.css
│   ├── layout.js
│   └── page.js
├── package.json
├── next.config.js
└── .gitignore
```

---

## 🚀 STEP 1 — Vercel에 배포하기

### 1-1. 사전 준비물

1. **GitHub 계정** — 없으면 https://github.com 에서 가입 (무료)
2. **Vercel 계정** — https://vercel.com 에서 GitHub로 로그인 (무료)
3. **Anthropic API 키** — https://console.anthropic.com 에서 발급 (사용한 만큼 과금)

### 1-2. GitHub에 코드 올리기

가장 쉬운 방법은 GitHub 웹페이지에서 직접 업로드하는 거예요.

1. https://github.com/new 에서 새 저장소 생성
   - Repository name: `stockcheck` (원하는 이름)
   - Public 선택
   - **Create repository** 클릭
2. 만들어진 페이지에서 **uploading an existing file** 링크 클릭
3. ZIP을 푼 폴더 안의 **모든 파일과 폴더를 드래그**해서 업로드
   (ZIP 자체가 아니라 그 안의 내용물!)
4. 아래 **Commit changes** 클릭

### 1-3. Vercel에서 가져오기

1. https://vercel.com/new 접속
2. 방금 만든 GitHub 저장소(`stockcheck`)에서 **Import** 클릭
3. **Environment Variables** 섹션 펼치기 → 다음 환경변수 추가:
   - **Name**: `ANTHROPIC_API_KEY`
   - **Value**: 발급받은 API 키 붙여넣기 (`sk-ant-api...` 로 시작)
4. **Deploy** 클릭
5. 1~2분 기다리면 배포 완료 → 🎉

### 1-4. 배포된 주소 확인

배포가 끝나면 다음 형태의 주소가 발급됩니다:
```
https://stockcheck-xxx.vercel.app
```
이게 바로 **카카오 애드핏에 등록할 매체 URL**입니다.

> 💡 **커스텀 도메인을 쓰고 싶다면**: 가비아·후이즈 등에서 `.com` 도메인을 구매한 뒤 Vercel 프로젝트의 **Settings → Domains** 에서 연결하면 됩니다. 도메인 비용은 보통 연 1만~2만원입니다.

---

## 🔧 STEP 2 — 배포 후 꼭 수정할 부분

### 2-1. 운영자 이메일 변경

`app/about/page.js`와 `app/privacy/page.js` 파일에서:
```
contact@example.com
```
이 부분을 **본인 실제 이메일**로 바꿔주세요. 카카오 애드핏 심사 시 운영자 연락처를 확인합니다.

수정 방법은 GitHub 웹에서 파일을 열고 연필 아이콘(Edit) 클릭 → 수정 → Commit 하면, Vercel이 자동으로 재배포합니다.

---

## 📢 STEP 3 — 카카오 애드핏 매체 등록

### 3-1. 매체 정보 입력 (이제 이 정보를 그대로 입력하세요)

| 항목 | 입력값 |
|------|--------|
| **매체명** | `1분점검` (또는 원하는 이름) |
| **매체 URL** | Vercel에서 받은 주소 (예: `https://stockcheck-xxx.vercel.app`) |
| **매체 카테고리** | `금융` 또는 `정보/정보통신` |
| **매체 설명** | 한국 주식 종목의 최신 뉴스, 토론방 분위기, 위험 신호를 한 줄로 요약해주는 투자 전 점검 도구입니다. AI 기반으로 공개된 정보를 자동 수집·요약하여 개인투자자의 정보 탐색 시간을 줄여줍니다. |

### 3-2. 심사 통과를 위한 체크리스트

심사 신청 전에 아래 항목이 정상 작동하는지 확인하세요:

- ✅ 메인 페이지(`/`)가 정상 로드되고 종목 점검 기능이 작동
- ✅ 개인정보처리방침 페이지(`/privacy`) 접근 가능
- ✅ 이용약관 페이지(`/terms`) 접근 가능
- ✅ 소개 페이지(`/about`) 접근 가능
- ✅ 푸터에 위 3개 페이지 링크가 모두 표시됨
- ✅ 운영자 이메일이 실제 이메일로 변경되어 있음
- ✅ 면책 문구("투자 자문 아님 · 본인 책임")가 표시됨

### 3-3. 심사 기간

보통 **1~3 영업일** 내에 결과가 나옵니다. 반려될 경우 사유와 함께 안내가 오니 수정 후 재신청하면 됩니다.

---

## 💰 STEP 4 — 광고 코드 삽입 (심사 통과 후)

심사 통과 후 카카오 애드핏에서 **광고 단위(슬롯)**를 만듭니다. 4개 만드세요:

| 슬롯 ID (코드 안) | 권장 사이즈 |
|------|------|
| `top-banner` | 728×90 또는 반응형 |
| `in-article` | 반응형(728×90 권장) |
| `sidebar-top` | 300×250 |
| `sidebar-bottom` | 300×250 |
| `bottom-banner` | 728×90 |

각 슬롯마다 `DAN-XXXXXXXXXX` 형태의 광고 단위 ID를 발급받습니다.

### 4-1. AdSlot 컴포넌트 수정

`app/StockCheck.js` 파일의 `AdSlot` 함수를 다음으로 교체하세요:

```jsx
const KAKAO_AD_UNITS = {
  "top-banner":     "DAN-XXXXXXXXXX",
  "in-article":     "DAN-XXXXXXXXXX",
  "sidebar-top":    "DAN-XXXXXXXXXX",
  "sidebar-bottom": "DAN-XXXXXXXXXX",
  "bottom-banner":  "DAN-XXXXXXXXXX",
};

function AdSlot({ id, label = "광고", height = 90, variant = "default" }) {
  const isSquare = variant === "square";
  const isSponsored = variant === "sponsored";
  const adUnit = KAKAO_AD_UNITS[id];
  const width = isSquare ? 300 : 728;
  const adHeight = isSquare ? 250 : 90;

  return (
    <div data-ad-slot={id} style={{ width: "100%", margin: isSponsored ? "20px 0" : "16px 0" }}>
      <div style={{
        fontFamily: "JetBrains Mono, monospace", fontSize: 9, letterSpacing: "0.18em",
        textTransform: "uppercase", color: "#9B9B95", marginBottom: 6,
        textAlign: isSquare ? "center" : "left",
      }}>{label}</div>
      <div style={{ display: "flex", justifyContent: "center" }}>
        <ins className="kakao_ad_area" style={{ display: "none" }}
          data-ad-unit={adUnit}
          data-ad-width={width}
          data-ad-height={adHeight}>
        </ins>
      </div>
    </div>
  );
}
```

### 4-2. 카카오 SDK 추가

`app/layout.js` 파일의 `<head>` 안에 다음 한 줄을 추가:

```jsx
<script async src="https://t1.daumcdn.net/kas/static/ba.min.js"></script>
```

### 4-3. 다시 GitHub에 커밋

수정한 파일을 GitHub에 commit 하면 Vercel이 자동 재배포 → 광고가 노출되기 시작합니다.

---

## ⚠️ 주의사항

- **본인 광고 클릭 금지**: 절대 자기 사이트의 광고를 클릭하지 마세요. 즉시 계정 정지됩니다.
- **클릭 유도 금지**: "여기 클릭하세요" 같은 문구를 광고 옆에 두면 안 됩니다.
- **트래픽이 적으면 수익도 적음**: 일 방문자 100명 미만이면 월 수익이 1만원 이하일 수 있습니다.
- **API 비용 관리**: Anthropic API는 호출당 과금됩니다. 트래픽이 폭증하면 비용도 늘어나니 Anthropic 콘솔에서 사용량 알림을 설정하세요.

---

## 🆘 문제가 생겼을 때

| 증상 | 해결 방법 |
|------|-----------|
| 배포는 됐는데 점검 시 "API 키가 설정되지 않았습니다" | Vercel → 프로젝트 → Settings → Environment Variables 에서 `ANTHROPIC_API_KEY` 추가 → Deployments에서 Redeploy |
| 점검이 너무 느림 | 정상 동작입니다. 웹 검색 + AI 분석에 10~30초 걸립니다. |
| 카카오 애드핏 심사 반려 | 보통 콘텐츠 부족 또는 운영자 정보 미흡이 원인. 종목을 몇 번 직접 점검해서 결과 페이지가 다양해 보이게 하고, 이메일을 실제 이메일로 변경 후 재신청하세요. |
| 도메인 변경하고 싶음 | Vercel → Settings → Domains 에서 커스텀 도메인 연결 가능 |

문제가 계속되면 Vercel과 카카오 애드핏 각각의 공식 문서를 참고하세요:
- Vercel 문서: https://vercel.com/docs
- 카카오 애드핏: https://adfit.kakao.com
